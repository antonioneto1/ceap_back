<%= form_with(model: ceap, multipart: true)do |form| %>
  <% if ceap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ceap.errors.count, "error") %> prohibited this ceap from being saved:</h2>

      <ul>
        <% ceap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= file_field_tag :file_csv %>
   </div>
  <div class="field">
    <%= form.label :exercicio %>
    <%= form.number_field :exercicio %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
